# AGENTS.md

## Mission

このリポジトリは、AIを使った仮想通貨取引の研究・検証・運用を、再現可能かつ安全に行うための会社OSである。利益の最大化より、資本の保全、証拠の完全性、安全な停止を優先する。

## Sources of truth

- 全体像と依存方向: `ARCHITECTURE.md`
- 会社の不変原則: `docs/charter.md`
- 仮説と実験: `docs/research-policy.md`
- 取引所とデータの対応関係: `docs/venue-data-policy.md`
- 独立検証と昇格: `docs/validation-policy.md`
- 注文・資金の安全境界: `docs/risk-policy.md`
- 監視、障害、振り返り: `docs/operations.md`
- 機械可読な初期制限: `config/risk-limits.yaml`

方針とコードが矛盾する場合は安全側に倒し、作業を止めて矛盾を報告する。

## Required workflow

1. 変更対象に関係する正本文書を読む。
2. 仮説や実験なら、結果を見る前にID、根拠、評価指標、棄却条件を登録する。
3. 実装者と検証者の成果物を分離する。作成者は自作戦略を承認しない。
4. データ来歴、時点整合性、手数料、スプレッド、スリッページ、資金制約を明示する。
5. 関連テストと `./scripts/verify.sh` を実行する。
6. 失敗、不採用、欠測を削除せず、理由とともに残す。
7. 本番昇格、増額、リスク上限緩和は、人間の明示承認を得て別変更として扱う。

## Hard safety boundaries

- AIセッション、開発用スクリプト、テストから実注文を送信しない。
- 初期状態では本番取引を有効化しない。`live_trading_enabled` は常に `false` とする。
- 取引所APIキー、秘密鍵、シード、トークン、個人情報をコード、設定、テスト、ログ、プロンプトに保存しない。
- 本番注文経路にLLMの非決定的な判断を置かない。承認済み・固定済みの戦略コードと決定論的リスクエンジンだけを使う。
- リスク検査の失敗、欠損、古い価格、残高不一致、不明な状態では新規注文を拒否する。
- テストを通すために安全検査を弱めたり、現実の取引費用を省略したりしない。
- 本番デプロイ、実注文、API権限変更、資金移動は、このリポジトリの通常作業の範囲外とする。

## Engineering rules

- Pythonのクラス、メソッド、関数には、日本語でGoogleスタイルのdocstringを書く。引数、戻り値、送出例外などがある場合は、`Args:`、`Returns:`、`Raises:`等の該当セクションを記載する。
- テスト関数のdocstringは例外とし、何をテストしているかを日本語で簡潔に記載すればよい。テスト関数にはGoogleスタイルの各セクションを要求しない。
- 時刻は内部ではUTCを使い、入力元のタイムゾーンと変換を記録する。
- 金額、価格、数量にバイナリ浮動小数点を無批判に使わない。
- バックテストと本番で、シグナル定義、約定規則、会計規則を共有する。
- 乱数を使う検証はseedを固定・記録する。
- 外部データは取得時刻、対象期間、取得元、変換、ハッシュまたは版を追跡可能にする。
- 新しい依存関係、取引所、銘柄、時間足を追加するときは、理由とリスクを文書化する。
- 既存の成熟したOSSを優先して利用し、同等機能をゼロから再実装しない。採用前にライセンス、保守状況、テスト、権限、既知の制約を確認する。

## Definition of done

- 目的と変更範囲が文書化されている。
- 正常系、失敗系、安全停止のテストがある。
- 未来参照とバックテスト会計への影響を確認している。
- 再現コマンドと結果を残している。
- 未解決リスクを明記している。
- 本番挙動を変える変更は、ロールバック方法と人間の承認記録を持つ。
